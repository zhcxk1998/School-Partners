export interface ForumInfo {
  forumId: number,
  forumAvatar: string,
  forumAuthor: string,
  publishTime: string,
  timeAgo: number,
  forumImage: string,
  forumTitle: string,
  forumContent: string,
  forumLike: number,
  forumComment: number
}