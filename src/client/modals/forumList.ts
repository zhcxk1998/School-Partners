export interface ForumInfo {
  forumId: number,
  forumAvatar: string,
  forumAuthor: string,
  publishTime: number,
  forumImage: string,
  forumTitle: string,
  forumContent: string,
  forumLike: number,
  forumComment: number
}